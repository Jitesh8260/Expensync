# Expensync ML Service Environment Variables
# Copy this file to '.env' and fill in the values.

# Port for Flask service to run on
ML_PORT=5050

# MongoDB connection string (same database as server)
MONGO_URI=mongodb://localhost:27017/expensync
